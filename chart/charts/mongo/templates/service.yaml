apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongo.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 27017
  selector:
    app: {{ include "mongo.name" . }}

